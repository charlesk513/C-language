#include <stdio.h>

int main()
{
    // FILE *fptr;
    // fptr = fopen("file.txt", "r");

    // if (fptr != NULL)
    // {
    //     char content[1000];
    //     while (fgets(content, 1000, fptr)) // fgets reads on statement at a time and returns true
    //     {
    //         printf("%s", content);
    //     }
    // }
    // else
    // {
    //     printf("The file access is unsuccessful");
    // }
    // fclose(fptr); // close to free up space

    // FILE *file;

    // file = fopen("file.txt", "r");
    // char comment[1000];
    // if (file != NULL)
    // {
    //     while (fgets(comment, 1000, file))
    //     {
    //         printf("fgets: %s", comment);
    //     }

    // }
    // else
    // {
    //     printf("File not successfully stored");
    // }
    // fclose(file);
    return 0;
}